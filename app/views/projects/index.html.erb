<div>
  <h2 class="text-2xl font-bold text-center mb-4">
    Tivo
  </h2>
  <section class="mb-4 flex justify-center">
    <div class="mt-2">
      <div>
        <%= link_to "Crear proyecto", new_project_path, class: "button" %>
      </div>
    </div>
  </section>
  <section class="mt-4">
    <% if @projects.any? %>
      <%= render 'projects/list', projects: @projects %>
    <% else %>
      <h3 class="text-xl font-semibold mb-2">
       No hay proyectos disponibles
      </h3>
      <p class="text-gray-700 mb-4">
        No parece que haya proyectos disponibles para mostrar en este momento.
      </p>
      <p class="text-gray-700 mb-4">
        Por favor, inténtelo de nuevo más tarde o contáctenos para obtener más información.
      </p>
    <% end %>
  </section >