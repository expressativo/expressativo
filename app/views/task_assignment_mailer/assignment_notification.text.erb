Hola <%= @task_assignment.user.full_name || @task_assignment.user.email %>,

<%= @assigned_by.full_name || @assigned_by.email %> te ha asignado la tarea "<%= @task.title %>" en el proyecto <%= @project.title %>.

<% if @task.notes.present? %>
Notas: <%= ActionController::Base.helpers.strip_tags(@task.notes.to_s).squish %>
<% end %>

<% if @task.due_date.present? %>
Fecha límite: <%= l(@task.due_date, format: :long) %>
Agregar a Google Calendar: <%= @gcal_url %>
<% end %>

Ver tarea: <%= @task_url %>

---
Este es un correo automático de Expressativo. No respondas a este mensaje.