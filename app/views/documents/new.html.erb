<% content_for :title, @document.file? ? "Nuevo archivo" : "Nuevo documento" %>

<div class="max-w-4xl mx-auto  mb-6 ">
  <h1 class="font-bold text-4xl">
    <%= @document.file? ? "Nuevo archivo" : "Nuevo documento" %>
  </h1>

  <%= render "form", document: @document %>

  <%= link_to "Ir a lista de documentos", project_documents_path(@project), class: "button-outline" %>
</div>
