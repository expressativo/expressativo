<% if todo.errors.any? %>
  <div class="errors">
    <ul>
      <% todo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_with model: [project, todo], local: true do |form| %>
  <div class="mb-4">
    <%= form.label :name , class: "label"%>
    <%= form.text_field :name, class: "input", placeholder: "Describe esta lista de tareas" %>
  </div>
  <%= form.submit "Guardar", class: "button" %>
<% end %>
